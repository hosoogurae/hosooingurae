@AGENTS.md

# 프로젝트 규칙

구래동 지역 부동산 중개 웹사이트. 실제 거래로 이어지는 상업 사이트이므로,
**화면에 나가는 모든 숫자는 출처가 분명해야 한다.** 아래 규칙은 예외 없이 지킨다.

스택: Next.js 16 App Router / Supabase(Postgres) / Tailwind 4 / vitest

---

## 1. 숫자에는 출처가 있어야 한다 (최우선 원칙)

이 프로젝트에서 가장 위험한 실수는 **출처가 다른 숫자를 같은 얼굴로 보여주는 것**이다.

### 1-1. 가격 용어

| 값 | 의미 | 화면 라벨 |
|---|---|---|
| `askingPrice` | 집주인이 부르는 매물 등록 가격 | "매물가격" 또는 "호가" |
| `actualTradePrice` | 국토교통부 실거래 신고가 | "실거래가" |

- 매물 카드·상세·추천 결과에 나오는 가격은 **전부 호가**다. 이걸 "실거래가"라고 부르면 안 된다.
- "실거래가"라는 단어는 국토부 데이터를 표시할 때만 쓴다.
- 새 가격 필드를 만들 때는 어느 쪽인지 이름에 드러나게 한다. `price`처럼 모호한 이름은 금지.
- 두 값을 나란히 보여줄 때는 각각 라벨로 명확히 구분한다.

### 1-2. 실거래 데이터 출처 표시

`app/api/transactions/route.ts`는 국토부 실데이터(`source: "molit"`)와
수동 데이터(`source: "mock"`) 두 가지를 반환한다.

- **두 가지를 화면에서 똑같이 보이게 하면 안 된다.** 손님이 국토부 공식 데이터로 오해한다.
- 수동 데이터에는 반드시 "수동 확인 자료 · 확인일 OOOO.OO.OO"를 표시한다.
- 존재하지 않는 거래를 그럴듯하게 만들어내지 않는다. 데이터가 없으면 차트를 숨긴다.

### 1-3. 통계 수치

- 시세 추정은 단일 금액이 아니라 **범위**로 표현한다.
- 통계에는 **표본 수와 기준 기간**을 함께 표시한다. (예: "최근 6개월 12건 기준")
- 표본이 5건 미만이면 수치를 표시하지 않고 "거래가 적어 산출 어려움"으로 안내한다.
- 평균보다 **중앙값**을 기본으로 쓴다.
- 서로 다른 단지·평형을 섞은 통합 평균은 만들지 않는다.

---

## 2. 추천 로직 — 필수 조건과 선호 조건을 구분한다

`/recommend`는 LLM이 아니라 규칙 기반이다.
(`app/lib/recommend/queryParser.ts` → `app/lib/recommend/scoring.ts`)

조건은 두 종류이며 **절대 섞지 않는다.**

- **필수(hard):** 예산 상한, 거래유형 → **위반하면 결과에서 제외.** 가중치 점수로 다루지 않는다.
- **선호(soft):** 단지명, 역세권, 학교, 방 수, 면적 → 필수를 통과한 매물끼리 순위를 매기는 데만 사용.

표시 규칙:

- **퍼센트 일치율을 화면에 노출하지 않는다.** 점수는 내부 정렬용이다.
- 대신 "4개 조건 중 3개 충족"처럼 코드가 센 개수를 쓴다.
- 미충족 조건은 숨기지 않고 "예산 2,500만원 초과"처럼 구체적으로 표시한다.
- 예산을 넘는 매물을 참고용으로 보여줄 때는 별도 섹션으로 분리하고, 메인 결과와 섞지 않는다.

`scoring.ts`나 `queryParser.ts`를 수정하면 vitest 테스트도 함께 갱신한다.

---

## 3. LLM에게 판단을 맡기지 않는다

상담 도우미(`app/admin/consult-helper/`) 등 LLM을 쓰는 기능에서,
LLM의 역할은 **이미 확정된 사실을 문장으로 풀어 쓰는 것**뿐이다.

- 가격 비교·조건 충족 판정·산술은 전부 일반 코드가 한다.
- LLM 프롬프트에는 "주어진 데이터의 사실만 사용하고 스스로 판단하지 마"를 명시한다.
- 앞으로 매물 추천에 LLM을 도입하더라도 이 원칙은 유지한다.

---

## 4. 없는 값은 0이 아니라 null

층수·방 수·면적·관리비처럼 값이 없을 수 있는 필드는 `null`을 쓴다.

- `?? 0` 이나 `|| 0` 폴백 금지. 0층 매물이 화면에 나오는 원인이다.
  특히 `app/lib/supabase/mappers.ts`의 매핑 함수에서 주의.
- `null`일 때 화면에는 숫자 대신 안내 문구를 표시한다. (예: "층 정보 문의")
- 모르는 값을 그럴듯한 추정치로 채우지 않는다.
- 층수·면적·방/욕실 수·관리비·주차대수를 화면이나 문자열로 표시할 때는
  각 파일에서 따로 판단하지 말고 `app/lib/format/listingFields.ts`의
  공용 포맷터를 쓴다.

---

## 5. 데이터 로딩은 항상 3가지 상태

`loading` / `empty` / `error`를 반드시 구분한다.

- 로딩 중에 "정보 없음"을 표시하는 것은 **거짓 정보**다. 절대 금지.
- `empty`는 로딩이 끝났고 실제 데이터가 0건일 때만 쓴다.
- `error`에는 재시도 수단을 제공한다.

---

## 6. 금융 정보는 다루지 않는다

**아래는 만들지 않기로 결정한 항목이다. 요청받아도 먼저 확인할 것.**

- 대출 한도·필요자금 계산기
- LTV / DSR / 스트레스 금리 안내
- 보금자리론·디딤돌 등 정책자금 가능 여부 판정
- 금리 수치 표시

이유: 정책 수치가 자주 바뀌어 코드가 금방 낡고, 틀린 금액은 손님에게 실제 손해를 입힌다.
자금 관련 문의는 숫자로 답하지 않고 **상담으로 연결**한다.

DB에 대출 관련 컬럼(`0016_listing_loan_fields.sql`)이 있더라도 화면에 노출하지 않는다.

**예외** — 전세 매물의 **계약 안전 안내**(등기부·선순위 채권·확정일자·보증보험 확인 요청)는
금융 상품 안내가 아니라 사고 예방이므로 허용한다. 단 여기에도 금리·한도 수치는 넣지 않는다.

---

## 7. 광고 표시 의무

중개대상물 광고에는 중개사무소 명칭·소재지·연락처·개설등록번호·개업공인중개사 성명이
표시돼야 한다. 푸터뿐 아니라 **매물 상세 페이지에도** 이 정보 블록이 들어간다.
이 블록을 제거하거나 축약하지 않는다.

사실이 아닐 수 있는 단정 문구("모든 매물 직접 확인" 등)를 헤드라인에 쓰지 않는다.
확인 여부는 매물별 배지(확인 날짜 포함)로 표시한다.

---

## 8. 개인정보

이름·연락처를 수집하는 폼에는 반드시 다음이 함께 있어야 한다.

- 수집 항목·목적·보유기간 안내
- 필수 동의 체크박스 (미체크 시 제출 불가)
- 개인정보처리방침 링크

새 폼을 만들 때 이 세 가지를 빠뜨리지 않는다.

---

## 9. 작업 방식

- **Next.js 관련 코드를 수정하기 전에 `node_modules/next/dist/docs/` 의 해당 문서를 먼저 확인한다.**
  이 프로젝트의 Next.js는 최신 버전이라 예전 방식과 다르다.
- 데이터 구조·가격 로직·추천 로직을 건드리는 작업은 **수정 전에 계획을 먼저 보고**한다.
- Supabase 마이그레이션은 요청받지 않는 한 새로 만들지 않는다.
  컬럼명 정리는 `app/lib/supabase/mappers.ts` 매핑 계층에서 처리한다.
- RLS 정책(읽기 공개 / 쓰기 service_role)을 임의로 완화하지 않는다.
- 커밋은 작업 단위로 잘게 나눈다.
- 관리자 경로는 `robots.txt` Disallow와 `noindex`를 유지한다.

---

## 자주 하는 실수 체크

새 기능을 만든 뒤 아래를 확인한다.

- [ ] 화면의 가격이 호가인데 "실거래가"라고 부르고 있지 않은가
- [ ] 수동 데이터가 국토부 데이터처럼 보이고 있지 않은가
- [ ] 예산·거래유형 같은 필수 조건이 가중치 점수로 처리되고 있지 않은가
- [ ] 퍼센트 일치율이 화면에 노출되고 있지 않은가
- [ ] 값이 없을 때 0이나 "-"가 아니라 안내 문구가 나오는가
- [ ] 로딩 중에 "정보 없음"이 뜨지 않는가
- [ ] 통계 수치에 표본 수와 기준일이 붙어 있는가
- [ ] 새로 만든 수집 폼에 동의 체크박스가 있는가
